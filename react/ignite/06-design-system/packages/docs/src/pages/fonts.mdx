import { fonts } from '@ignite-ui/tokens';
import { Meta } from '@storybook/blocks';
import { TokensGrid } from '../components';

<Meta title="Tokens/Font Families" />

# Font Families

Essas são as familía de fonts utilizadas no Ignite UI.

<TokensGrid tokens={fonts} hasRemValue/>